# Core (installs native arm64 builds on Apple Silicon)
torch>=2.2
snntorch>=0.9.1
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65

# Real dataset access (PhysioNet). Optional — the pipeline runs on synthetic data without it.
wfdb>=4.1

# Xylo deployment + bit-accurate simulator (SynSense Rockpool). Optional — only
# needed for the hardware-target path in eia/rockpool_models.py.
# Install with the simulator backend:  pip install "rockpool[xylo]"
rockpool>=3.1
